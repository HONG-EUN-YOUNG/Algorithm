-- 방법 1
SELECT DATETIME
FROM (
    SELECT DATETIME
    FROM ANIMAL_INS
    ORDER BY DATETIME DESC
)
WHERE ROWNUM = 1

-- 방법 2
/*
SELECT DATETIME
FROM ANIMAL_INS
WHERE DATETIME = (
    SELECT MAX(DATETIME)
    FROM ANIMAL_INS
)
*/
